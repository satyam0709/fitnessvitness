Fix Today feed timezone bug, meeting visibility, and upcoming section correctness.

- Frontend sends ?date=YYYY-MM-DD using browser local timezone so server
  uses the correct calendar day instead of UTC date (fixes meetings appearing
  in Upcoming instead of Today for IST users)
- Dashboard Upcoming Schedule widget also uses local timezone for from/to
  dates and isToday label comparison
- fetchMeetings (today) now dynamically checks is_deleted column to avoid
  SQL errors on databases without the column
- Both fetchMeetings and fetchUpcomingMeetings include meeting_attendees
  subquery so attendees see the meeting, not just organizer/assignee
- Fix canViewAll ReferenceError in fetchUpcomingCollectionFollowups by
  inlining the role check instead of calling an undefined function
- Fix client_followup is_overdue: was hardcoded 1 (always Overdue), now
  uses CASE WHEN so due-today items show in Today section correctly
- Add fetchUpcomingLeadFollowups so future lead follow-ups appear in Upcoming
- Limit upcoming display to top 10 (was 50); realtime mark-done auto-rotates
- Rename Upcoming Tasks section label to Upcoming
- Rebrand 365 RND CRM to FitnessVitness CRM across frontend and backend
- Add dark theme support for Fitness CRM sections and dashboard
