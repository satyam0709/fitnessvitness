export const metadata = {
  title: "Create workspace – RND TECHNOSOFT CRM",
  description: "Sign up for a dedicated CRM workspace, choose a plan, and pay securely.",
};

export default function SignupLayout({ children }) {
  return children;
}
